package com.efgh.algorithms;

public class Utils {

	/**
	 * A palindrome is a word that reads the same if read backwards, i.e. ALA,
	 * SUGUS, ANITALAVALATINA
	 * 
	 * This function shall return true if the word is a palindrome, false if
	 * isn't, please consider:
	 * 
	 * 1. A null or empty String is a invalid input
	 * 
	 * 2. For the test purpose, pretend that you will not receive spaces in your
	 * input
	 * 
	 * 3. You shall not change any current call to this function
	 * 
	 * @param wordToCheck
	 *            i.e. ANA
	 * @return true if the word is a palindrome, false if isn't
	 */
	public static boolean isPalindrome(String wordToCheck) {
		return true;
	}

	/**
	 * A prime number is a number greater than 1 that can only be divisible by 1
	 * and itself.
	 * 
	 * This function shall return true if the given number is a prime, false if
	 * isn't, please consider:
	 * 
	 * 1. Negative values are invalid inputs
	 * 
	 * 2. You shall not change any current call to this function
	 * 
	 * @param numberToCheck
	 *            i.e. 3
	 * @return true if the number is a prime, false if isn't
	 */
	public static boolean isPrimeNumber(int numberToCheck) {
		return true;
	}

	/**
	 * You will receive a camel case word, you should count the quantity of
	 * upper case letter on it
	 * 
	 * 1. A null or empty String is a invalid input
	 * 
	 * 2. For the test purpose, pretend that you will not receive spaces nor
	 * special characters in your input
	 * 
	 * 3. You shall not change any current call to this function
	 * 
	 * @param stringToCheck
	 *            i.e. thisAwesomeWordHasFour
	 * @return amount of upper case letters in the string
	 */
	public static int camelCaseCount(String stringToCheck) {
		return -2;
	}

	/**
	 * We want to add the digits of a big number and return the result of the
	 * sum
	 * 
	 * 1. Negative values (and zero) are invalid inputs
	 * 
	 * 2. You shall not change any current call to this function
	 * 
	 * @param number
	 *            i.e. 1234
	 * @return result of the added values
	 */
	public static int sumDigits(int number) {
		return -2;
	}

	/**
	 * This function shall return the sum of the 2 smallest , please consider:
	 * 
	 * 1. A null, empty String, non number characters, are invalid inputs
	 * 
	 * 2. For the test purpose, pretend that you will not receive spaces nor
	 * special characters in your input
	 * 
	 * 3. You shall not change any current call to this function
	 * 
	 * @param commaSeparatedNumbers
	 *            i.e. 1,2,3,4
	 * @return the sum of the smallest 2 numbers on the string
	 */
	public static long smallestSum(String commaSeparatedNumbers) {
		return -2;
	}

	/**
	 * echo a comma separated string of numbers from 1 to the given number, the
	 * tricky part: replace all multiples of 3 by fizz and all multiples of 5 by
	 * buzz, if they are multiples of both put fizzbuzz
	 * sum
	 * 
	 * 1. Negative values and Zero are invalid inputs
	 * 
	 * 2. You shall not change any current call to this function
	 * 
	 * @param maxNumber
	 *            i.e. 100
	 * @return the string containing the numbers, i.e. 1,2,fizz,4,buzz...
	 */
	public static String fizzBuzz(int maxNumber) {
		return null;
	}
}
