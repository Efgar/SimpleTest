package com.vp.utils;

public class Utils {

	
	/**
	 * A palindrome is a word that reads the same if read backwards, i.e. ALA, SUGUS, ANITALAVALATINA
	 * 
	 * This function shall return true if the word is a palindrome, false if isn't, please consider:
	 * 
	 * 1. A null or empty String is a invalid input
	 * 2. For the test purpose, pretend that you will not receive spaces in your input
	 * 3. You shall not change any current call to this function
	 * 
	 * @param wordToCheck
	 * @return true if the word is a palindrome, false if isn't
	 */
	public static boolean isPalindrome(String wordToCheck){
		return true;
	}
	
}
